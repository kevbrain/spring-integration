apiVersion: route.openshift.io/v1
kind: Route
metadata:
  annotations:
    openshift.io/host.generated: "true"
  labels:
    app: metrics-app-h2
    group: metrics-app-h2
    provider: jkube
  name: metrics-app-h2
spec:
  host: metrics-launcher-infra.apps.ksr.clusters.its4u.eu
  port:
    targetPort: http
  to:
    kind: Service
    name: metrics-app-h2
    weight: 100
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Allow
  wildcardPolicy: None
status: